# Apps already installed by hand are "adopted" instead of failing the install
cask_args adopt: true

# CLI
brew "biome"
brew "fzf"
brew "gh"
brew "git"
brew "mise"
brew "pnpm"
brew "zoxide"

# Fonts
cask "font-fira-code"
cask "font-fira-code-nerd-font"

# Apps

# -- dev / terminal --
cask "1password-cli"
cask "cmux"
cask "cursor"
cask "figma"
cask "linear"

# -- productivity --
cask "1password"
cask "notion"
cask "notion-calendar"
cask "raycast"
cask "setapp"

# -- communication --
cask "discord"
cask "slack"
cask "whatsapp"

# -- media / utilities --
cask "bambu-studio"
cask "google-chrome"
cask "helium-browser"
cask "keka"
cask "logi-options+"
cask "spotify"
cask "stremio"
cask "synology-drive"
cask "vlc"

# -- AI / assistants --
cask "claude"

# No cask — install by hand
# Affinity (com.canva.affinity, v3.2.1, "Identified Developer"): Canva's unified
#   app (2025) has no cask of its own. The "affinity-designer", "affinity-photo"
#   and "affinity-publisher" casks in homebrew-cask are the old v2 suite
#   (deprecated, disabled on 2026-10-30) — a different product.
# Hubble (com.benholmes.hubblemd.desktop) — no cask in homebrew-cask.
# mymind (com.mymind.mymind-mac) — no cask; installed via the App Store.
# Paper (com.todesktop.2601167vjw8xe) — no cask. Homebrew's "paper" cask is
#   "pap.er" (wallpapers), an unrelated product.
# Pencil (dev.pencil.desktop) — no cask. Homebrew's "pencil" cask is Evolus
#   Pencil (prototyping GUI), a different product and deprecated anyway
#   (blocked by Gatekeeper, disabled on 2026-09-01).
# Ports (com.atapper.Ports) — no cask in homebrew-cask.

# vscode (not applicable: Cursor is not VS Code; extensions sync with the account)
# vscode "anthropic.claude-code"
# vscode "anysphere.remote-ssh"
# vscode "biomejs.biome"
# vscode "fey.oscura"
