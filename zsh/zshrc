# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

typeset -U path PATH
path=(
  "$HOME/.local/bin"
  $path
)
export PATH

for file in \
  ~/.config/zsh/10-options.zsh \
  ~/.config/zsh/20-navigation.zsh \
  ~/.config/zsh/30-aliases.zsh \
  ~/.config/zsh/40-functions.zsh \
  ~/.config/zsh/50-completion.zsh \
  ~/.config/zsh/60-prompt.zsh \
  ~/.config/zsh/70-tools.zsh \
  ~/.config/zsh/90-plugins.zsh; do
  [[ -f "$file" ]] && source "$file"
done

# pnpm
export PNPM_HOME="$HOME/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
